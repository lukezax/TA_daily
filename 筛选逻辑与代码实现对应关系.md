# 筛选逻辑与代码实现对应关系

## 1. B1波段原始条件公式

### 888.docx中的逻辑

```
N:=9;
M1:=14; M2:=28; M3:=57; M4:=114;
RNG:=HHV(H,N)-LLV(L,N);
RSV:=IF(RNG=0,50,(C-LLV(L,N))/RNG*100);
K:=SMA(RSV,3,1);
D:=SMA(K,3,1);
J:=3*K-2*D;
ZXDQ:=EMA(EMA(C,10),10);
ZXDKX:=(MA(C,M1)+MA(C,M2)+MA(C,M3)+MA(C,M4))/4;
振幅:=(HIGH-LOW)/REF(C,1)*100;
涨幅:=(C-REF(C,1))/REF(C,1)*100;
MA60:=MA(C,60);
倍量柱条件:=COUNT(V/REF(V,1)>=2,250)>=1;
流通市值条件:=FINANCE(41)>800000;
总市值条件:=FINANCE(40)>1000000;
XG: J<13 AND C>MA60 AND C>ZXDKX AND ZXDQ>ZXDKX
    AND 振幅<7 AND 涨幅>=-2 AND 涨幅<2
    AND 倍量柱条件 AND 流通市值条件 AND 总市值条件;
```

### 代码实现对应关系

| 逻辑项 | 代码实现位置 | 实现说明 |
|-------|------------|--------|
| N:=9 | filter.py:calculate_rsv | 函数参数 n=9 |
| M1~M4 | filter.py:b1_strategy_filter | 直接使用这些值计算ZXDKX |
| RSV | filter.py:calculate_rsv | (C - LLV(L,N)) / (HHV(H,N) - LLV(L,N)) * 100，RNG=0时返回50 |
| K/D/J | filter.py:calculate_kdj | K=2/3*K_prev+1/3*RSV；D=2/3*D_prev+1/3*K；J=3K-2D |
| ZXDQ | filter.py:b1_strategy_filter | EMA(EMA(C,10),10)，即白线 |
| ZXDKX | filter.py:b1_strategy_filter | (MA14+MA28+MA57+MA114)/4，即黄线 |
| 振幅 | filter.py:b1_strategy_filter | (HIGH-LOW)/REF(C,1)*100 |
| 涨幅 | filter.py:b1_strategy_filter | (C-REF(C,1))/REF(C,1)*100 |
| MA60 | filter.py:b1_strategy_filter | 60日简单移动平均 |
| 倍量柱条件 | filter.py:b1_strategy_filter | 250天内至少1天 V/REF(V,1)>=2 |
| 流通市值条件 | filter.py:b1_strategy_filter | 实时接口获取 lt（元），与 800000万元（80亿）比较 |
| 总市值条件 | filter.py:b1_strategy_filter | 实时接口获取 sz（元），与 1000000万元（100亿）比较 |
| n-1日K在黄白值之间 | filter.py:b1_strategy_filter | min(黄线,白线) <= 前日收盘价 <= max(黄线,白线) |
| n-1周K高于白线 | filter.py:b1_strategy_filter | 前一周收盘价 > 周K白线（EMA(EMA(C,10),10)） |

## 2. B1策略新增4个评分条件

每个条件满足得1分，总分0~4，按总分降序排列。**不影响B1筛选结果。**

### 通达信公式

```
COND1 := COUNT(VOL/REF(VOL,1)>=2,30)>=1;
MAX_VOL_120 := HHV(VOL,120);
COND2 := COUNT(VOL>MAX_VOL_120*0.75,120)=0;
RATE_2DAY := (C/REF(C,2)-1)*100;
RATE_3DAY := (C/REF(C,3)-1)*100;
COND3 := COUNT(RATE_2DAY>40,30)=0 AND COUNT(RATE_3DAY>50,30)=0;
DAY_HS := HSCOL;
WEEK_HS := SUM(HSCOL,5);
COND4 := COUNT(DAY_HS>15,30)=0 AND COUNT(WEEK_HS>40,30)=0;
```

### 代码实现对应关系

| 条件 | 代码变量 | 实现说明 |
|-----|---------|---------|
| COND1：30日倍量 | `cond1_satisfied` | 遍历最近30天，统计 V/REF(V,1)>=2 的次数，>=1则满足 |
| COND2：120日无大量 | `cond2_satisfied` | 用 volumes[-240:-120] 的最大量作基准，检查 volumes[-120:] 内有无超过基准75%的天；需至少240天数据 |
| COND3：涨幅控制 | `cond3_satisfied` | 遍历最近30天，检查2日/3日涨幅违规次数均为0 |
| COND4：换手率控制 | `cond4_satisfied` | 用实时数据 lt/p 计算流通股本，再用历史 v（手）估算换手率；无实时数据时默认满足 |

**COND2实现说明：** 原始公式中 `HHV(VOL,120)` 若与检查窗口相同，最大值本身必然超过自身75%，条件永远不成立。正确实现为双窗口：基准期取第121~240天前的最大量，检查期取最近120天，两个窗口不重叠。

**COND4实现说明：** 通达信 `HSCOL` 在历史数据中不可直接获取。通过实时数据的 `lt`（流通市值元）/ `p`（价格）计算流通股本，再用历史 `v`（成交量，手，1手=100股）估算每日换手率。

## 3. B1策略新增6个统计项

不影响B1筛选，仅展示true/false。

| 统计项 | 代码变量 | 计算逻辑 |
|-------|---------|---------|
| 日K3天斜率<0.2 | `stat1_slope_lt_02` | (close[-1]-close[-4])/(close[-4]*3)*100 < 0.2 |
| 黄白gap>5% | `stat2_gap_gt_5pct` | abs(黄线-白线)/close[-1]*100 > 5.0 |
| 斜率三条件 | `stat3_cond` | 30日K斜率≤1 且 周K斜率>0 且 周K斜率<30日K斜率（周K用实际可用根数，最多120根） |
| 30天无连续涨停 | `stat4_no_consecutive_limit_up` | 最近30天内无连续两日收盘价≥前收盘价×1.099 |
| 无长上影 | `stat5_no_long_upper` | 30日K和周K中均无：上影线>实体×2 且 上影线>振幅×30% |
| 30天涨幅达标 | `stat6_within_limit` | 30天涨幅在总市值对应档位内（100~400亿<15%，400~1000亿<10%，≥1000亿<6%）；总市值不在范围或无数据时为None |

## 4. B1策略新增3个形态统计项

不影响B1筛选，仅展示true/false。检查最近120天数据，True=无该形态。

| 统计项 | 代码变量 | 触发条件（全部满足才算出现形态） |
|-------|---------|-------------------------------|
| 无顶部大风车 | `stat7_no_top_windmill` | ①当日最高价≥前30日最高价×90% ②上影线>实体×2 ③当日成交量>近5日均量×1.5 ④当日为阴线 |
| 无非正常放量 | `stat8_no_abnormal_volume` | ①当日成交量>近20日均量×3 ②次日成交量<当日×50% ③当日为阴线 |
| 无跳空 | `stat9_no_gap` | 向上跳空：当日开盘价>前日最高价；向下跳空：当日最高价<前日最低价 |

## 5. 超短线游击战法

### 888.docx中的逻辑

```
VAR1A:=(HHV(HIGH,4)-CLOSE)/(HHV(HIGH,4)-LLV(LOW,4))*100-90;
VAR2A:=SMA(VAR1A,4,1)+100;
VAR3A:=(CLOSE-LLV(LOW,4))/(HHV(HIGH,4)-LLV(LOW,4))*100;
VAR4A:=SMA(VAR3A,6,1);
VAR5A:=SMA(VAR4A,6,1)+100;
VAR6A:=VAR5A-VAR2A;
砖型图:=IF(VAR6A>4,VAR6A-4,0);
昨日绿柱:=REF(砖型图,1) < REF(砖型图,2);
今日红柱:=砖型图 > REF(砖型图,1);
当日绿转红:=昨日绿柱 AND 今日红柱;
红柱达标:=今日红柱高度 >= 昨日绿柱高度*1.5 AND 昨日绿柱高度>0 AND 今日红柱高度>0;
均线多头:=MA20>MA60 AND MA60>MA120;
量能条件:=COUNT(每日条件=0,10)=0;
XG:当日绿转红 AND 红柱达标 AND 均线多头 AND 量能条件;
```

### 代码实现对应关系

| 逻辑项 | 代码实现位置 | 实现说明 |
|-------|------------|--------|
| VAR1A~砖型图 | filter.py:ultra_short_strategy_filter | 逐步计算各中间变量 |
| 当日绿转红 | filter.py:ultra_short_strategy_filter | brick[-2]<brick[-3] AND brick[-1]>brick[-2] |
| 红柱达标 | filter.py:ultra_short_strategy_filter | 今日红柱高度 >= 昨日绿柱高度 × 1.5 |
| 均线多头 | filter.py:ultra_short_strategy_filter | MA20[-1] > MA60[-1] > MA120[-1] |
| 量能条件 | filter.py:ultra_short_strategy_filter | 最近10天，阴线日成交量均小于前一日 |
| 日K高于白线或黄线 | filter.py:ultra_short_strategy_filter | close[-1] > white_line OR close[-1] > yellow_line |
| 白线在黄线之上 | filter.py:ultra_short_strategy_filter | white_line > yellow_line |
| 价格不低于黄线 | filter.py:ultra_short_strategy_filter | close[-1] >= yellow_line |

## 6. 数据来源

| 数据类型 | 本地缓存路径 | 主要字段 |
|---------|------------|---------|
| 日K数据 | stock_data/{code}_d.json | o/h/l/c/v（开高低收量） |
| 周K数据 | stock_data/{code}_w.json | o/h/l/c/v |
| 实时数据 | stock_data/{code}_realtime_d.json | sz=总市值(元), lt=流通市值(元), p=价格, hs=换手率% |
