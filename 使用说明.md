# 股票筛选工具使用说明

## 工具介绍

本工具是一个基于Python的股票筛选系统，根据888.docx中的筛选逻辑，通过调用智兔API获取股票数据，计算技术指标，并筛选出符合条件的股票。

## 名词解释

### 技术指标

| 名词 | 全称/含义 | 计算方式 |
|-----|---------|---------|
| KDJ | 随机指标 | 基于N日内最高/最低/收盘价计算RSV，再平滑得到K、D、J三条线 |
| J值 | KDJ中的J线 | J = 3K - 2D，反映超买超卖，J<13为超卖区域 |
| MA60 | 60日简单移动平均线 | 最近60个交易日收盘价的算术平均值 |
| EMA | 指数移动平均线 | 对近期数据赋予更高权重的移动平均，multiplier = 2/(period+1) |
| ZXDQ（白线） | EMA(EMA(C,10),10) | 收盘价的10日EMA再做一次10日EMA，反映中期趋势 |
| ZXDKX（黄线） | (MA14+MA28+MA57+MA114)/4 | 四条均线的平均值，反映长期趋势中枢 |
| 振幅 | 当日价格波动幅度 | (最高价 - 最低价) / 前日收盘价 × 100% |
| 涨幅 | 当日价格变动幅度 | (收盘价 - 前日收盘价) / 前日收盘价 × 100% |
| 倍量柱 | 成交量是前一日2倍以上的K线 | 当日成交量 / 前日成交量 >= 2 |
| 换手率 | 当日成交股数占流通股本的比例 | 成交量(手)×100 / 流通股本 × 100% |
| 上影线 | K线最高价到实体顶部的距离 | 最高价 - max(开盘价, 收盘价) |
| 实体 | K线开盘价到收盘价的距离 | \|开盘价 - 收盘价\| |
| 长上影线 | 上影线显著长于实体的K线 | 上影线 > 实体×2 且 上影线 > 振幅×30% |
| 跳空缺口 | 相邻两日价格区间无重叠 | 向上跳空：当日开盘>前日最高；向下跳空：当日最高<前日最低 |
| 顶部大风车 | 高位阴量长上影形态 | 阶段高位+长上影+放量+阴线，四个条件同时满足 |
| 非正常放量 | 阴量暴量后次日缩量 | 当日成交量>近20日均量×3 且 次日缩量<50% 且 当日为阴线 |
| 斜率 | K线趋势的倾斜程度 | (最新收盘价 - N天前收盘价) / (N天前收盘价 × N) × 100，单位：%/天 |
| 黄白gap | 黄线与白线的差值 | \|ZXDKX - ZXDQ\| / 收盘价 × 100% |
| 砖型图 | 超短线策略的核心指标 | 基于4日高低价和收盘价计算的动量指标 |

### CSV结果字段说明

| 字段名 | 含义 |
|-------|------|
| 结果 | 是否符合B1原始11个条件（符合/不符合） |
| 原始B1满足 | 同上，true/false格式 |
| 新增条件总分 | 4个评分条件满足的数量（0~4分） |
| 新增条件1_30日内倍量 | 30日内是否有至少1天成交量是前一日2倍 |
| 新增条件2_120日内无大量卖出 | 最近120天内无单日成交量超过基准期最大量75% |
| 新增条件3_30日内涨幅控制 | 30日内无连续2日涨幅>40%且无连续3日涨幅>50% |
| 新增条件4_30日内换手率控制 | 30日内单日换手率≤15%且单周换手率≤40% |
| 5天回测收益率 | 过去5个交易日的净涨幅（%） |
| 日K3天斜率<0.2 | 最近3天日K斜率是否小于0.2%/天 |
| 黄白gap>5% | 黄白线差值是否超过股价5% |
| 斜率三条件 | 30日K斜率≤1 且 周K斜率>0 且 周K斜率<30日K斜率 |
| 30天无连续涨停 | 最近30天内无连续两个涨停板 |
| 无长上影 | 30日K和周K均无长上影线 |
| 30天涨幅达标 | 30天涨幅在对应总市值档位限制内 |
| 无顶部大风车 | 120日内无阴量顶部大风车形态 |
| 无非正常放量 | 120日内无阴量暴量+次日缩量形态 |
| 无跳空 | 120日内无向上或向下跳空缺口 |
| 总市值 | 公司总市值（元），来自实时数据 |
| 流通市值 | 公司流通市值（元），来自实时数据 |
| n-1日收盘价 | 前一个交易日的收盘价 |
| 到黄线距离 | 前日收盘价与黄线的绝对差值 |

## 安装指引

```bash
pip install requests numpy
```

环境要求：Python 3.7+，稳定网络连接。

## 项目结构

```
stock/
├── api.py          # API调用模块
├── config.py       # 配置文件
├── filter.py       # 筛选逻辑模块
├── mock_data.py    # 模拟数据模块
├── stock_filter.py # 主脚本
├── data_storage.py # 数据存储模块
├── retry_manager.py # 失败列表管理模块
└── stock_data/     # 本地K线缓存目录
```

## 配置文件说明

### B1波段策略参数 (`B1_STRATEGY_CONFIG`)

| 配置项 | 默认值 | 说明 |
|-------|-------|------|
| `n` | 9 | KDJ计算周期 |
| `m1/m2/m3/m4` | 14/28/57/114 | ZXDKX均线周期 |
| `ema_period` | 10 | ZXDQ的EMA周期 |
| `ma60_period` | 60 | MA60周期 |
| `j_threshold` | 13 | J值阈值（J<13才满足） |
| `amplitude_threshold` | 7 | 振幅阈值(%)（振幅<7才满足） |
| `change_min_threshold` | -2 | 涨幅下限(%) |
| `change_max_threshold` | 2 | 涨幅上限(%) |
| `volume_multiplier` | 2 | 倍量柱倍数 |
| `volume_check_days` | 250 | 倍量柱检查天数 |
| `min_circulating_market_cap` | 800000 | 最小流通市值（万元，即80亿） |
| `min_total_market_cap` | 1000000 | 最小总市值（万元，即100亿） |

### 时间范围配置 (`TIME_RANGE_CONFIG`)

| 配置项 | 默认值 | 说明 |
|-------|-------|------|
| `days` | 1300 | 历史数据获取天数（需足够长以支持120周K和评分条件2的双窗口） |
| `start_date` | None | 开始日期，None表示自动计算 |
| `end_date` | None | 结束日期，None表示自动取前一天 |

### 其他配置

| 配置项 | 默认值 | 说明 |
|-------|-------|------|
| `STORAGE_CONFIG.use_local_data` | True | 是否优先使用本地缓存 |
| `STORAGE_CONFIG.max_data_age_hours` | 24 | 本地数据有效期（小时） |
| `CONCURRENCY_CONFIG.max_workers` | 2 | 并发线程数 |

## 命令行参数说明

| 参数 | 默认值 | 说明 |
|-----|-------|------|
| `--strategy` | b1 | 筛选策略：b1 或 ultra_short |
| `--test` | False | 测试模式，只处理前10个股票 |
| `--output` | file | 输出方式：screen 或 file（带时间戳的CSV） |
| `--mock` | False | 使用本地缓存数据，不调用API |
| `--retry` | False | 只处理 failed_stocks.json 中的失败股票 |

## 使用示例

```bash
# 正式运行，B1策略，结果输出到CSV，并自动推送飞书
python stock_filter.py --strategy b1

# 测试模式，只处理前10只股票（同样会推送飞书）
python stock_filter.py --test --strategy b1

# 使用本地缓存数据（不消耗API次数）
python stock_filter.py --mock --strategy b1

# 重试上次失败的股票
python stock_filter.py --retry
```

## 飞书通知

B1筛选完成后会自动将符合条件的股票推送到飞书群。每只股票包含以下信息：
- 股票代码、名称、交易所
- 收盘价、涨幅、振幅
- J值、MA60、黄线、白线
- 总市值、流通市值
- 新增条件得分（0~4分）
- 5天回测收益率

超过10只时自动分批发送。无符合条件时发送"今日无符合条件的股票"提示。

飞书通知配置在 `feishu_notify.py` 中（`FEISHU_WEBHOOK` 和 `FEISHU_SECRET`）。

## 数据存储机制

1. 股票历史数据缓存在 `stock_data/` 目录，有效期24小时
2. 实时数据缓存有效期1小时
3. API调用失败的股票记录到 `failed_stocks.json`，可用 `--retry` 重新处理
4. 脚本启动时自动清空上次的失败记录（retry模式除外）

**刷新本地缓存：** 删除 `stock_data/` 目录下对应的 `.json` 文件，或将 `max_data_age_hours` 改小。

## 注意事项

1. **排除范围**：自动排除创业板（300/301/302开头）、科创板（688开头）和ST股票，只处理沪深主板
2. **API限制**：智兔API每天调用次数有限，建议正常运行时保持 `use_local_data=True`
3. **数据量要求**：评分条件2需要至少240天日K，斜率三条件需要足够的周K数据，`days=1300` 已满足这些需求
4. **缓存刷新**：修改 `days` 配置后需删除旧缓存才能生效
5. **换手率条件**：历史数据无直接换手率字段，通过实时数据估算；无实时数据时条件4默认满足
6. **市值条件**：无实时数据时市值条件默认满足（不排除）
7. **30天涨幅达标**：总市值<100亿或无实时数据时结果为空（None）
8. **优雅退出**：支持 Ctrl+C 中断，会取消剩余任务

## 常见问题

**Q：筛选结果为空？**
可能没有股票同时满足11个原始B1条件，这是正常的。可以查看CSV中各条件的true/false分布来判断哪个条件最严格。

**Q：如何调整筛选条件？**
修改 `config.py` 中的对应参数，例如调整 `j_threshold`、`amplitude_threshold` 等。

**Q：如何更新本地数据？**
删除 `stock_data/` 目录下的对应 `.json` 文件，或将 `max_data_age_hours` 改为0。

**Q：斜率三条件全是False？**
需要足够的周K数据。确认 `config.py` 中 `days=1300`，并删除旧缓存重新拉取数据。

**Q：评分条件2全是False？**
需要至少240天日K数据（双窗口基准）。同上，确认 `days=1300` 并刷新缓存。
