# 股票筛选系统架构文档

## 1. 系统架构概述

股票筛选系统是一个基于Web的应用，用于根据特定策略筛选股票。系统采用前后端分离的架构，后端使用Flask框架提供API服务，前端使用HTML/CSS/JavaScript实现用户界面。

## 2. 系统组件

### 2.1 后端组件

1. **Flask应用**：作为Web服务器，处理HTTP请求和响应
2. **API模块**：负责与外部股票数据API交互，获取股票数据
3. **筛选模块**：实现B1波段和超短线游击战法的筛选逻辑
4. **认证模块**：预留用户注册、登录功能
5. **调度器**：实现定时执行筛选任务

### 2.2 前端组件

1. **首页**：展示系统状态和提供手动执行按钮
2. **结果页面**：展示筛选结果，支持策略选择
3. **历史页面**：展示执行历史记录

### 2.3 数据存储

1. **本地文件**：存储股票数据、执行历史记录和用户数据
2. **内存存储**：临时存储筛选结果和执行状态

## 3. 系统流程

### 3.1 手动执行流程

1. 用户访问首页，点击执行按钮
2. 前端发送请求到 `/api/filter` 接口
3. 后端接收请求，获取股票列表
4. 后端对每只股票执行筛选逻辑
5. 后端保存筛选结果和执行历史
6. 前端更新状态并跳转到结果页面

### 3.2 自动执行流程

1. 调度器在每天08:30触发执行任务
2. 后端执行B1和超短策略的筛选
3. 后端保存筛选结果和执行历史
4. 结果可在历史页面查看

## 4. API接口

### 4.1 筛选接口

- **POST /api/filter**：触发筛选，参数为策略类型
- **GET /api/results/<strategy>**：获取筛选结果
- **GET /api/status**：获取执行状态
- **GET /api/history**：获取执行历史

### 4.2 认证接口

- **POST /api/auth/register**：用户注册
- **POST /api/auth/login**：用户登录
- **GET /api/auth/user/<username>**：获取用户信息
- **POST /api/auth/logout**：用户注销

## 5. 技术栈

- **后端**：Python 3.9+, Flask 2.0.1, APScheduler 3.9.1
- **前端**：HTML5, CSS3, JavaScript
- **数据处理**：Pandas 1.3.3
- **网络请求**：Requests 2.26.0
- **容器化**：Docker, Docker Compose

## 6. 部署架构

### 6.1 本地开发环境

- 直接运行 `python app.py` 启动应用
- 访问 http://localhost:5000

### 6.2 Docker容器化部署

- 使用 `docker build` 构建镜像
- 使用 `docker run` 或 `docker-compose up` 运行容器
- 映射端口 5000:5000

### 6.3 公网部署

- 在云服务器上安装Docker
- 拉取代码并构建镜像
- 配置Nginx反向代理（可选）
- 配置域名和SSL证书（可选）

## 7. 扩展能力

### 7.1 功能扩展

- 支持更多筛选策略
- 实现完整的用户管理系统
- 添加数据可视化图表
- 集成更多股票数据API

### 7.2 性能优化

- 使用缓存减少API调用
- 实现异步处理提高性能
- 优化数据库查询（当使用数据库时）

## 8. 安全考虑

- 密码加密存储（预留）
- API访问控制（预留）
- 输入验证和参数检查
- 错误处理和日志记录