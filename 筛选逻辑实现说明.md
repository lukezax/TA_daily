# 股票筛选系统实现说明

## 项目结构

```
stock/
├── api.py            # API调用模块，负责与智兔API交互
├── config.py         # 配置文件
├── filter.py         # 筛选逻辑模块，实现B1波段和超短线游击战法
├── mock_data.py      # 模拟数据模块，用于测试
├── stock_filter.py   # 主脚本，命令行交互界面
├── data_storage.py   # 数据存储模块
├── retry_manager.py  # 失败列表管理模块
├── 888.docx          # 筛选逻辑和解释
├── 智兔API文档.docx   # API调用方法
└── 筛选逻辑实现说明.md  # 本说明文档
```

## 筛选逻辑实现位置

### 1. B1波段原始条件公式

**实现位置：** `filter.py` 中的 `b1_strategy_filter` 函数

**核心逻辑（11个原始条件，全部满足才算符合B1）：**
- J < 13（KDJ的J值）
- 收盘价 > MA60
- 收盘价 > ZXDKX（黄线）
- ZXDQ（白线）> ZXDKX（黄线）
- 振幅 < 7%
- 涨幅 >= -2%
- 涨幅 < 2%
- 倍量柱条件（250天内至少1天成交量是前一天2倍以上）
- 流通市值 > 80亿元，总市值 > 100亿元（通过实时API获取）
- n-1日K在黄白值之间
- n-1周K高于白线

**新增4个评分条件（不影响B1筛选，每满足1个得1分，按总分降序排列）：**
- 条件1：30日内至少1天成交量是前一日2倍
- 条件2：最近120天内无单日成交量超过「第121~240天前最大成交量」的75%
- 条件3：30日内连续2日涨幅≤40%且连续3日涨幅≤50%
- 条件4：30日内单日换手率≤15%且单周换手率≤40%

**新增6个统计项（不影响B1筛选，仅展示true/false）：**
- 日K3天斜率<0.2
- 黄白gap>5%
- 斜率三条件（30日K斜率≤1 且 120日周K斜率>0 且 120日周K斜率<30日K斜率）
- 30天无连续涨停
- 无长上影（30日K和周K均无：上影线>实体×2 且 上影线>振幅×30%）
- 30天涨幅达标（按总市值分档）

**新增3个形态统计项（不影响B1筛选，仅展示true/false）：**
- 无顶部大风车（120日内无阴量顶部大风车形态）
- 无非正常放量（120日内无阴量暴量+次日缩量形态）
- 无跳空（120日内无向上或向下跳空缺口）

**5天回测：** 对所有处理的股票计算过去5个交易日净涨幅，输出到CSV。

### 2. 超短线游击战法

**实现位置：** `filter.py` 中的 `ultra_short_strategy_filter` 函数

**核心逻辑：**
- 砖型图当日绿转红
- 红柱高度 >= 昨日绿柱高度 × 1.5
- 均线多头（MA20 > MA60 > MA120）
- 最近10个交易日，阴线当日成交量小于前一日
- 日K高于白线或黄线
- 白线在黄线之上
- 价格不低于黄线

## API调用实现

**实现位置：** `api.py`

**主要接口：**
- `get_stock_list()`: 获取沪深A股股票列表
- `get_stock_realtime_data(stock_code)`: 获取实时数据（含换手率hs、流通市值lt、总市值sz、价格p）
- `get_stock_history_data(stock_code, period, limit)`: 获取历史K线数据（period: d=日线, w=周线）

**数据时间范围：** `config.py` 的 `TIME_RANGE_CONFIG.days = 1300`，确保能获取到：
- 120周K（约840天）用于斜率三条件
- 240天日K用于评分条件2的双窗口基准

## 技术实现细节

### KDJ指标计算

- `calculate_rsv`: RSV = (C - LLV(L,N)) / (HHV(H,N) - LLV(L,N)) * 100，N=9
- `calculate_kdj`: K = 2/3 * K_prev + 1/3 * RSV；D = 2/3 * D_prev + 1/3 * K；J = 3K - 2D

### 均线计算

- `calculate_ma`: 简单移动平均
- `calculate_ema`: 指数移动平均，multiplier = 2 / (period + 1)

### 评分条件2的双窗口逻辑

原始公式 `COUNT(VOL > HHV(VOL,120) * 0.75, 120) = 0` 中，若基准和检查窗口相同，最大值本身必然超过自身75%，条件永远不成立。

正确实现：用 `volumes[-240:-120]`（第121~240天前）的最大量作为基准，检查 `volumes[-120:]`（最近120天）内有没有超过该基准75%的天。两个窗口不重叠，逻辑成立。需要至少240天日K数据。

### 长上影判断标准

同时满足以下两个条件才算长上影：
1. 上影线 > 实体 × 2（上影线 = 最高价 - max(开盘价, 收盘价)；实体 = |开盘价 - 收盘价|）
2. 上影线 > 振幅 × 30%（振幅 = 最高价 - 最低价）

### 跳空判断标准

- 向上跳空：当日开盘价 > 前一日最高价（整个价格区间在前日之上）
- 向下跳空：当日最高价 < 前一日最低价（整个价格区间在前日之下）

### 斜率三条件的周K数据

周K斜率用实际可用的周K数量计算（最多120根，至少2根），不硬要求121根。斜率公式：`(close[-1] - close[-n-1]) / (close[-n-1] * n) * 100`。

## 注意事项

1. **API调用限制**：智兔API每天调用次数有限，正常运行时优先使用本地缓存（24小时有效）
2. **市值数据**：通过实时接口 `/hs/real/ssjy/{code}` 获取，无实时数据时市值条件默认满足
3. **换手率（条件4）**：历史数据无直接换手率字段，通过实时数据的 `lt/p` 计算流通股本后估算；无实时数据时条件4默认满足
4. **30天涨幅达标**：总市值<100亿或无实时数据时结果为 `None`
5. **本地缓存刷新**：修改 `days` 配置后需删除 `stock_data/` 目录下的旧缓存，才能重新拉取更长时间范围的数据

## 后续优化方向

1. 增加更多的筛选策略
2. 优化KDJ和其他指标的计算精度
