# 模型并发调度方案(消费者模型)

> 最终版 v4。本文档是实施与 TDD 验收的唯一依据。

## 1. 目标

把当前 pipeline 里互斥的三套模式(`--deep` / `--lite` / 默认"假 local")替换为**统一消费者模型**:

- 股票作为**生产者**,进入单一 FIFO 队列。
- 本地、云端作为**消费者 worker**,各自从队列抢股票,谁先抢到谁先跑。
- 谁先完成就消费谁的结果,写入报告。**不存在主次、不存在股票与模型的绑定。**
- 失败不重试,重新入队(最多重试 1 次)。
- 某个模型连续失败 3 次,判定该模型**本轮失效**,本轮不再参与消费;下一轮(下一次 pipeline.run)默认恢复。

## 2. 心智模型

```
股票队列 (queue.Queue, FIFO, 元素 = 一只股票)
  [s1, s2, s3, ..., sN]
        │           │
        │           └── cloud worker 抢到 s2,开跑
        └── local worker 抢到 s1,开跑

worker 跑完一只股:
  成功 → 该股离开队列(永不回),结果写 analysis_data[code]
  失败 → 该股重新入队(可被任一 worker 再抢),该 worker 失败 streak +1

worker 连续 3 次失败 → 该 worker 本轮失效,不再抢任务(它手里当前持有
的那只股放回队列,给仍然存活的 worker 抢)
```

**关键不变量**:
1. 一只股在任意时刻最多被一个 worker 持有(`queue.Queue.get()` 原子互斥保证)。
2. 一只股的总尝试次数 ≤ `max_retry + 1`(默认 2 次)。
3. 失败 streak 计数在 **worker 维度**,不跨 worker、不跨股票。
4. worker 失效是**本轮 scope**:`StockDispatchQueue` 实例销毁即重置。
5. `analysis_data[code]` 采用**后写覆盖**(先失败后成功的场景,成功覆盖失败)。

## 3. 数据结构

### 3.1 `pipeline/config.py`

```python
@dataclass
class WorkerSpec:
    """一个 worker = 一种模型调用通道(本地 / 云端)。"""
    name: str                    # "local" / "cloud"
    model_name: str              # 传给 TA-CN 的 model_name
    timeout: int = 10800         # 单只股超时(秒)

@dataclass
class DispatchConfig:
    max_retry: int = 1           # 失败重入队次数(0=不重试;1=总共尝试 2 次)
    failure_threshold: int = 3   # 同一 worker 连续失败 N 次判本轮失效

@dataclass
class PipelineConfig:
    # ...现有字段全部保留...
    workers: List[WorkerSpec] = field(default_factory=list)
    dispatch: DispatchConfig = field(default_factory=DispatchConfig)
```

### 3.2 `pipeline/models.py`

```python
@dataclass
class WorkerOutcome:
    """单只股 × 单 worker 的旁路结果(不进报告,仅日志/汇总)。"""
    code: str
    worker_name: str
    status: str                  # completed / failed / dead_skipped
    recommendation: str = ""
    confidence_score: float = 0.0
    risk_score: float = 0.0
    summary: str = ""
    error_message: str = ""
    execution_time: float = 0.0
    attempt: int = 0
```

`PipelineResult` 扩展字段(原有字段保留):

```python
    primary_by: Dict[str, str] = field(default_factory=dict)   # {code: worker_name}
    extras: List[WorkerOutcome] = field(default_factory=list)  # 旁路记录
    dead_workers: List[str] = field(default_factory=list)      # 本轮失效的 worker
```

`StockAnalysisResult` **不改**(报告渲染依赖它,约束"报告不改")。

## 4. 核心模块 `pipeline/stock_dispatch.py`

`StockDispatchQueue` 类,职责:

- `submit_all(stocks)`:把所有股票(每只 attempt=0)放入单一 `queue.Queue`。
- `run(on_result, on_extra, on_all_done)`:启动 N 个 worker(N = `len(workers)`),每个 worker 一个线程。
- worker 循环:登录 → 抢任务 → 跑 → 成功/失败 → 继续抢,直到队列空或自身失效。
- `_on_success`:streak 归 0,调 `on_result(stock, result, worker_name, attempt)`。
- `_on_failure`:
  - streak +1;若 streak ≥ `failure_threshold` → 标 dead,当前股永久丢弃(dead_skipped),不入队。
  - 若 attempt ≥ `max_retry` → 永久失败(丢弃)。
  - 否则 → 重新入队(attempt+1),任何存活 worker 可抢。
- 看门狗:主线程 `queue.join()` 等待;若所有 worker 都 dead 且队列非空 → 剩余任务全部标记 dead_skipped 丢弃,防止死锁。

## 5. `pipeline/analysis_client.py`

- **新增** `submit_one(stock, model_name, headers) -> task_id`:`POST /api/analysis/batch` 提交单只股,`quick_analysis_model = model_name`。
- **新增** `poll_one(task_id, headers, timeout, deadline) -> StockAnalysisResult`:轮询到 completed/failed/timeout,保留 401 重登录逻辑。
- **删除**:`process_single_batch` / `_process_batch` / `analyze_batch` / `deep_mode` / `lite_mode` / 硬编码 `quick_analysis_model="local"` / `deep_analysis_model="qwen3.7-max"`。
- **保留**:`_login` / `_fetch_result` / `_normalize_code`。
- 新增 `self._deadline` 字段(由 orchestrator 在执行前注入)。

## 6. `pipeline/orchestrator.py`

- `run()` 删除 `deep_mode` / `lite_mode` 参数,删除原重试阶段(orchestrator.py:251-294)。
- 替换为:

```python
queue = StockDispatchQueue(workers=config.workers, dispatch_cfg=config.dispatch,
                           executor=self.analysis_client)
queue.submit_all(filter_results.stocks)
queue.run(on_result=..., on_extra=..., on_all_done=...)
```

- `on_result(stock, result, worker_name, attempt)`:`analysis_results[stock.code] = result`(后写覆盖),`primary_by[stock.code] = worker_name`,触发 `report_generator.generate()` 增量更新。
- `PipelineScheduler` 删除 `deep_mode` / `lite_mode` 参数及传递。
- `_run_researcher`(策略研究员)**不动**。

## 7. `pipeline/main.py`

- argparse 删除 `--deep` / `--lite`。
- 调度器初始化不再传 deep/lite。

## 8. `pipeline/report_generator.py` —— **一字不改**

`analysis_data: Dict[str, StockAnalysisResult]` 形状不变。报告渲染逻辑不变。

## 9. `pipeline_config.yaml`

```yaml
pipeline:
  # ...现有字段不变...
  # 新增:
  workers:
    - name: local
      model_name: "Qwen3.5-122B-A10B-w4a8-nothinking"
      timeout: 10800
    - name: cloud
      model_name: "qwen3-max"
      timeout: 1800
  dispatch:
    max_retry: 1
    failure_threshold: 3
```

## 10. TA-CN 端 —— **代码不改,仅配置**

在 TA-CN Web 后台"大模型配置"注册两个 model_name(前置动作,非代码):
- `Qwen3.5-122B-A10B-w4a8-nothinking`(provider=custom_openai, base_url=http://100.127.30.80:22222/v1, api_key=sk-local)
- `qwen3-max`(provider=dashscope, base_url=百炼, api_key=DASHSCOPE_API_KEY)

## 11. 行为对照表

| 场景 | 行为 |
|---|---|
| N 股 × 2 worker | 队列长度 N,两个 worker 抢。谁先完成谁的结果写 analysis_data |
| local 跑某股失败 | 该股回队列,cloud 可抢。cloud 成功 → analysis_data[code] = cloud 结果 |
| 同股两 worker 都失败 | attempt 0 失败回队列,attempt 1 再失败 → 永久丢弃,extras 记录 |
| local 连续失败 3 次(任意 3 只股) | local 标 dead,不再抢任务。cloud 继续跑剩余股 |
| 所有 worker 都 dead | 看门狗触发,剩余股全部 dead_skipped 丢弃 |
| 报告 | analysis_data[code] = 后写覆盖的结果。报告与现状视觉一致 |
| 下一轮执行 | dead worker 状态重置,重新参与 |

## 12. TDD 验收标准(测试用例)

| # | 用例 | 预期 |
|---|---|---|
| T1 | submit_all 入队后队列长度 = 股数 | 队列元素 = 股数,每只 attempt=0 |
| T2 | 单 worker 单股成功 | on_result 被调 1 次,analysis_data 有该股结果,streak=0 |
| T3 | 单股失败 1 次后成功(attempt 0 失败 → 入队 → 成功) | on_result 最终被调,extras 无 failed 记录,总尝试 2 次 |
| T4 | 单股连续失败超过 max_retry | 该股永久 failed,入 extras,不再入队 |
| T5 | 某 worker 连续 3 次失败 → dead | 该 worker 停止抢任务,剩余任务由活 worker 处理 |
| T6 | 所有 worker 都 dead | 看门狗触发,剩余股 dead_skipped,不死锁 |
| T7 | 报告渲染 | analysis_data 形状不变(StockAnalysisResult),report_generator 无改动可正常渲染 |
| T8 | deep/lite 移除 | main.py / orchestrator.py / analysis_client.py 中不再有 deep_mode/lite_mode 符号 |

## 13. 实施顺序

1. (前置)TA-CN Web 后台注册两个 model_name。
2. `pipeline/config.py` + `pipeline_config.yaml`。
3. `pipeline/models.py`。
4. 新建 `pipeline/stock_dispatch.py`。
5. `pipeline/analysis_client.py`。
6. `pipeline/orchestrator.py` + `pipeline/main.py`。
7. 写 `test_stock_dispatch.py`,跑通 T1-T8,对照本文档验收。
